language: java
install: true

os: linux
dist: trusty
jdk: oraclejdk11

before_install:
  - chmod +x gradlew
  - sudo apt-get -qq update
  - sudo apt-get install pocl

script:
  - ./gradlew test --info