language: java
install: true

os: linux
dist: trusty
jdk: oraclejdk11


before_install:
  - chmod +x gradlew
  - sudo apt-get -qq update

script:
  - ./gradlew test --info
  - ./gradlew check
  - ./gradlew codeCoverageReport
  
after_success:
  - bash <(curl -s https://codecov.io/bash)
